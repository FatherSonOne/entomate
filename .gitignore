# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files (contain secrets)
.env
.env.local
.env.*.local
.env.production
.env.staging
.env.development
backend/.env
backend/.env.local
backend/.env.production
backend/.env.staging
frontend/.env
frontend/.env.local
frontend/.env.production
frontend/.env.staging

# Claude Code local settings
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS files
Thumbs.db

# Build outputs
build/
coverage/

# Temporary files
*.tmp
*.temp
.cache/

# Windows reserved file names
nul
NUL
con
CON
aux
AUX
prn
PRN

# Secret/key files
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.cert
*.crt
serviceAccount*.json
*-credentials.json
firebase-adminsdk*.json
google-cloud-key*.json
*.secret
*secret*.json
*secrets*.json
*credentials*.yaml
*credentials*.yml

# 2FA / TOTP / backup codes (per-account secret material) — pattern matches
# any path so accidental drops anywhere in the repo are blocked.
*[Bb]ackup-[Cc]odes*
*[Bb]ackup_[Cc]odes*
*totp*
*TOTP*

# SSH keys
id_rsa
id_rsa.pub
id_ed25519
id_dsa

# Infrastructure secrets
terraform.tfstate
terraform.tfstate.backup
*.tfvars
!*.tfvars.example

# Database files
*.sqlite
*.sqlite3
*.db

.vercel

# nano-banana temp URI files
*.uri

.vercel
.env*.local
