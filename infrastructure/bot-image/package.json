{
  "name": "entomate-bot",
  "version": "0.2.0",
  "private": true,
  "description": "Entomate meeting bot — Puppeteer-driven headless Chromium capturing meeting audio. Runs as a per-session Fly.io Machine.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build:docker": "docker build -t entomate-bot:local ."
  },
  "dependencies": {
    "otplib": "^12.0.1",
    "puppeteer-core": "^23.6.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "engines": {
    "node": ">=20"
  }
}
