{
  "buildCommand": "cd frontend && npm install && npm run build && mv dist/index.html dist/app.html && mkdir -p dist/logos && cp -r ../output/logos/. dist/logos/ && cp ../output/landing/index.html dist/index.html",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/app.html" }
  ]
}
